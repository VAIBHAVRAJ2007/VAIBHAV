#include <stdio.h>
int main()
{
    int arr[] = {14,58,47,74,1};
    // printf("first element of array = %d\n", arr[0]);
    // printf("first element of array = %d\n", *arr);
    // printf("second element of array = %d\n", arr[1]);
    // printf("second element of array = %d\n", *(arr + 1));
    // printf("third element of array = %d\n", *(arr + 2));
    // printf("address of first element of array = %u\n", arr);
    // printf("address of second element of array = %u\n", &arr[1]);
    // printf("address of second element of array = %u\n", arr + 1);
    // printf("address of third element of array = %u\n", &arr[2]);
    // printf("address of third element of array = %u\n", arr + 2);
    //  int *x = arr;
    // printf("first element of array = %d\n", *x);
    // //  *(x + 0) = (*(x + 0) + 10) / 4 ;
    // //  *(x + 1) = (*(x + 1) + 10) / 4 ;
    // //  *(x + 2) = (*(x + 2) + 10) / 4 ;
    // //  *(x + 3) = (*(x + 3) + 10) / 4 ;
    // //  *(x + 4) = (*(x + 4) + 10) / 4 ;
    //  for (int i = 0; i < 5; i++)
    //      printf("%d ", *(x + i));
    
     return 0;
}
